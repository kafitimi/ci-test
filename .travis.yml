language: python
python:
  - "3.6"
  - "3.4"
# command to install dependencies
# install:
# - pip install -r requirements.txt
#
# command to run tests
script: python -m unittest tests/test_algebraic.py
branches:
  only:
    - sergeev.vs
