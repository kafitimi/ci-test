language: python
python:
  - "3.6"
  - "3.4"
# command to install dependencies
# install:
# - pip install -r requirements.txt
#
# command to run tests
script: python tests/test_algebraic.py
branches:
  only:
    - sergeev.vs
